steps:
  - name: gcr.io/cloud-builders/git
    entrypoint: bash
    args:
      - '-c'
      - |
        git checkout master && \
        git pull origin master --unshallow && \
        git checkout $COMMIT_SHA && \
        git diff --quiet HEAD~2 $COMMIT_SHA ${_COMPARISON}
